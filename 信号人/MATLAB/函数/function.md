# <center>概述
一般而言：
- 一个函数文件中只包含一个函数
- 函数名称为 函数文件名称

# <center>形式

### 单一量：
```
function 输出对象 = 函数文件名a(输入对象1)
    关系式
end
```
使用：temp = a(1)


### 多值
```
function [输出对象1，输出对象2] = 函数文件名a(输入对象1，输入对象2)
    关系式
end
```
使用：[temp1,temp2] = a(1,2)

